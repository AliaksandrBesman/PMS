<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.bstu.besman.lw6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LW6">
        <activity android:name=".UserAcceptanceCreationActivity"></activity>
        <activity android:name=".UserContactInfoActivity" />
        <activity android:name=".UserEducationInfoActivity" />
        <activity android:name=".UserCountryInfoActivity" />
        <activity android:name=".UserNameInfoActivity" />
        <activity android:name=".UserDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>